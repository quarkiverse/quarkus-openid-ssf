# Delivery axis: PUSH
# Activate with -Dquarkus.profile=push

quarkus.openid-ssf.receiver.delivery-method=PUSH
quarkus.openid-ssf.receiver.push.endpoint-path=/ssf/push
# Optional shared secret enforced on the inbound Authorization header.
#quarkus.openid-ssf.receiver.push.expected-auth-header=${SSF_RECEIVER_PUSH_EXPECTED_AUTH_HEADER:}
